"use strict";

exports.__esModule = true;
exports.writeFileContent = exports.readFileContent = exports.isCloudBuild = void 0;

var _path = require("path");

var fs = _interopRequireWildcard(require("fs"));

function _getRequireWildcardCache(nodeInterop) {


const readFileContent = async path => {
  return fs.promises.readFile(path, 'utf-8');
};

exports.readFileContent = readFileContent;

const writeFileContent = async (path, content) => {
  await fs.promises.mkdir((0, _path.dirname)(path), {
    recursive: true
  });
  await fs.promises.writeFile(path, content, 'utf-8');
};

exports.writeFileContent = writeFileContent;

const isCloudBuild = env => {
  return env.NODE_ENV === 'production' && env.CI === 'true';
};

exports.isCloudBuild = isCloudBuild;